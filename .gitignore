/tmp
/out-tsc

/node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
/.pnp
.pnp.js

.idea*
.DS_Store

dist
.next
out

# TypeScript's incremental build cache. Pure build output, rewritten by every
# `tsc`/`next build`, and it was tracked by mistake — untrack it with
# `git rm --cached tsconfig.tsbuildinfo`, or this line does nothing.
*.tsbuildinfo

# Cypress artifacts
cypress/screenshots
cypress/videos
cypress/downloads

# Claude Code personal settings (CLAUDE.md itself stays tracked)
.claude/settings.local.json